const CustomError = require("../extensions/custom-error");

module.exports = function createDreamTeam( /*members*/ ) {
  throw new CustomError('Not implemented');
    // remove line with error and write your code here
    
   /*
 let arr_length = members.length;
 const l = members.length;
 let result;
 const regex = /[A-Z]/g;
 let sor = members.filter( item => typeof item == 'string');
 let time = sor.join(', ').match(regex);
 if (time.length > arr_length){
   let b = time.filter(function(item, index){
     return index % 2 == 0;
   })
  }
 
 if (sor.length > 0 && time > arr_length){
   b.sort().join('').split();
   return b;
 } else if (sor.length > 0 && time < arr_length) {
  time = time.sort().join('').split();
  return time;
 } else {
   return false;
 }
 
 

 

 if (Array.isArray(members) && members.length > 0 && sor.length > 0 && time > 0 && time == arr_length){
  time = time.join('').split();
  return time;
  
  //.join('').split();
    }


   */
 
  
}

  

